现在这个部署方式**仍然需要 `frontend/dist`，但不需要把它提交到 Git**。

`frontend/dist` 的作用是：`npm run build` 生成静态前端资源，然后 `npx wrangler@4 deploy` 会把 `frontend/dist` 上传到 Cloudflare Workers Static Assets。  
所以部署流程是：

```powershell
npm run deploy:worker
```

它会自动：

1. 构建 `frontend/dist`
2. 上传 Worker
3. 上传静态前端资源

**可以删的本地内容**
这些都可以删，删了不影响源码，之后能重新生成：

- `dist/`：旧的根目录构建产物，现在不用了。
- `frontend/dist/`：当前前端构建产物，可以删；下次部署会重新生成。
- `node_modules/`：依赖目录，可以删；之后 `npm install` 恢复。
- `output/`：Playwright 截图、日志、浏览器 profile，可能含登录态，本地调试完可删。
- `.wrangler/`：Wrangler 本地缓存，可以删。
- `apimonitor-dashboard-readme.png`：临时截图，README 已使用 `docs/assets/dashboard.png`。
- `references/`：下载的参考源码，可以删；不影响当前项目运行。
- `.tmp-dashboard-*.png`：如果还有，也可以删。

**建议保留**
这些是项目源码或文档，不要删：

- `frontend/`，但不含 `frontend/dist/`
- `worker/`
- `supabase/`
- `tests/`
- `docs/`
- `.github/`
- `README.md`
- `.env.example`
- `.gitignore`
- `AGENTS.md`
- `package.json`
- `package-lock.json`
- `tsconfig.json`
- `vitest.config.ts`
- `wrangler.toml`

**特别注意**
`.env` 不提交，但本地建议保留。里面有真实 token/cookie/secrets，删了之后本地 e2e 和重新上传 secrets 会麻烦。  
不过云端已经有 Worker secrets，所以线上运行不依赖你本地 `.env`。