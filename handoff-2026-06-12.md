# CONTEXT HANDOFF — DO NOT SUMMARIZE, DO NOT COMPRESS

### 1. 任务状态（Task State）

- 当前目标：API 与 Coding Plan 用量聚合看板 — 已部署“阿里云百炼稳妥入口版”，并把设置页规划记录整理到本 handoff，下一阶段再做设置页 UI/UX 对齐。
- 已完成步骤：
  - 项目脚手架搭建：commit `9f09b67`
  - 可配置供应商 + 多账号 + 凭据加密 + 阿里云百炼初版：commit `7978697`
  - Cloudflare Worker + Assets 已部署：`https://apimonitor.jarvislee90s.workers.dev`
  - 最新部署版本：`26acad0d-c6b8-4aed-a4c6-8eecd8e52ce6`
  - Supabase 数据库 5 个 migration 已执行
  - 线上端到端验证：
    - OpenRouter：`ready`，4 个 windows
    - OpenCode Go：`ready`，3 个 windows
    - 讯飞 MaaS：`ready`，3 个 windows
    - 阿里云百炼：`partial`，0 windows，作为原网页入口显示
  - 本地验证：
    - `npm run test` 通过，49 tests passed
    - `npm run build` 通过，仅 Vite chunk size warning
  - 阿里云百炼真实网页抓包已确认页面存在订阅：
    - 页面显示 `Coding Plan Pro`
    - 状态：生效中
    - 抓包曾读到用量：近 5 小时 0%、近一周 3%、近一月约 20%/21%
  - 已确认阿里云百炼纯 Worker HTTP 复放不稳定：
    - 即使上传 `ALIYUN_BAILIAN_AUTH_COOKIE`
    - 上传 `ALIYUN_BAILIAN_API_URL`
    - 上传 `ALIYUN_BAILIAN_SEC_TOKEN`
    - 线上仍返回登录页/登录态失效
  - 已改为稳妥版：
    - `worker/providers/aliyun-bailian.ts` 默认不发云端抓取请求
    - 默认返回 `partial` 入口卡片，summary 为“阿里云百炼需要打开原网页查看；登录态由浏览器保存”
    - `meta.openMode = "external"`
    - 实验性云端抓取保留，但必须显式配置 `ALIYUN_BAILIAN_CLOUD_FETCH=1`
  - 已修复线上配置合并问题：
    - Supabase active account 配置不完整时，会与 Worker env/secrets fallback 合并
    - 修复位置：`worker/index.ts`
- 进行中步骤：
  - 无正在运行的命令
  - 下一阶段待做：设置页 UI/UX 继续对齐截图规划
- 阻塞项：
  - 阿里云百炼不能可靠通过 Cloudflare Worker 纯 HTTP 抓取结构化 usage；当前产品决策为“原网页入口型 provider”
  - Cloudflare Browser Run / Browser Rendering 登录修复仍为后置复杂方案，当前未实现

### 2. 关键决策与假设（Decisions & Assumptions）

- 为什么选阿里云百炼稳妥入口版而非继续云端抓取：
  - 真实 Playwright 登录页面能抓到百炼数据，说明用户订阅存在
  - 但相同 cookie/API/sec_token 在 Worker 中仍被阿里云判定为登录页
  - 继续做纯 HTTP 复放会长期不稳定，并污染首页状态
  - 因此现阶段把百炼作为“统一入口 + 浏览器保存登录态”的 provider，更符合可用性
- 当前百炼行为：
  - 默认 `partial`
  - 不计入 `login_required`
  - 不主动向阿里云 API 发请求
  - 用户点击原网页入口后，依赖当前浏览器的阿里云登录态
- 实验性百炼云端抓取：
  - 仍保留在 `worker/providers/aliyun-bailian.ts`
  - 只有 `cloudFetchEnabled` 为 true、或 Worker env `ALIYUN_BAILIAN_CLOUD_FETCH=1` 时启用
  - 云端抓取即使返回登录页，也会回退为入口卡片，避免拖垮首页
- 设置页规划记录：
  - 方案名称：`C 安全增强版 / B 式体验`
  - 核心思路：独立配置页，敏感凭据只在 Worker/Supabase 加密保存，前端只展示脱敏信息
  - 体验目标：像方案 B 一样灵活管理 provider、账号、启用状态和顺序
  - 安全目标：不要把 cookie/API key/密码暴露给前端或文档
- 已确认环境：
  - Node.js/npm 可用
  - Wrangler v4 可部署
  - Supabase project ref：`cdzjuvewreribelvpccz`
  - Worker 域名：`apimonitor.jarvislee90s.workers.dev`
  - Hash routes：`#/` 首页，`#/settings` 配置页
  - Durable Object：`RefreshSessionDurableObject`
- 尚未验证的假设：
  - ⚠️ OpenCode Go / 讯飞 MaaS cookie 长期有效性
  - ⚠️ 设置页当前实现和截图 mock 的视觉/交互是否完全一致
  - ⚠️ Cloudflare Browser Rendering 是否能稳定承载百炼登录修复

### 3. 文件与代码状态（File & Code State）

- 已修改文件：
  - `worker/providers/aliyun-bailian.ts`
    - 重写为干净 UTF-8
    - 默认返回原网页入口卡片
    - 保留实验性 API 抓取路径
    - 抓取失败回退为入口卡片
  - `worker/index.ts`
    - 新增 provider config 合并逻辑 `mergeProviderConfig`
    - DB active account 配置优先，但空值/缺失字段由 env fallback 补齐
    - 阿里云百炼新增 `cloudFetchEnabled` 传递
  - `worker/types.ts`
    - 新增 `ALIYUN_BAILIAN_SEC_TOKEN`
    - 新增 `ALIYUN_BAILIAN_CLOUD_FETCH`
  - `tests/worker/providers.test.ts`
    - 新增/调整百炼默认入口模式测试
    - 保留实验性云端抓取解析测试
    - 登录页/NotLogined 回退入口卡片测试
  - `tests/worker/index.test.ts`
    - 新增 DB 配置不完整时合并 env secrets 的回归测试
    - 实验性百炼抓取测试显式启用 `cloudFetchEnabled`
- 已存在设置页相关文件：
  - `frontend/src/settings/settings-page.tsx`
  - `frontend/src/settings/provider-order-list.tsx`
  - `frontend/src/settings/provider-account-panel.tsx`
  - `frontend/src/settings/credential-form.tsx`
  - `frontend/src/App.tsx`
  - `frontend/src/components/dashboard-shell.tsx`
  - `frontend/src/api/client.ts`
  - `frontend/src/styles.css`
- 设置页测试：
  - `tests/frontend/settingsPage.test.tsx`
  - `tests/frontend/credentialForm.test.tsx`
  - `tests/frontend/apiClient.test.ts`
- 计划文档：
  - `docs/superpowers/plans/2026-06-12-configurable-providers-and-bailian.md`
  - 该计划对应截图里的“独立配置页：C 安全增强版 / B 式体验”
- 当前未跟踪/调试文件：
  - `get-api-detail.cjs`
  - `get-api.cjs`
  - `get-cookies.cjs`
  - `get-cookies.js`
  - `tests/e2e/real-provider-snapshots.test.ts`
  - `output/playwright/*` 下有百炼抓包/登录调试脚本和截图
- 关键变量/常量：
  - `REFRESH_COOLDOWN_MS = 120000`
  - `REFRESH_ACTIVE_WINDOW_MS = 600000`
  - Provider IDs：`openrouter | opencode-go | xfyun-maas | aliyun-bailian`
  - 百炼默认入口：`https://bailian.console.aliyun.com/cn-beijing?tab=plan#/efm/subscription/coding-plan`
  - 百炼实验抓取开关：`ALIYUN_BAILIAN_CLOUD_FETCH=1`
- 未提交变更摘要：
  - 5 个 tracked files modified
  - 多个调试脚本 untracked
  - 本 handoff 文件 untracked/updated

### 4. 待办与下一步（Next Actions）

- 下一步必须做：
  1. 打开当前前端 `#/settings`，对照用户截图里的设置页 mock 做 UI/UX review
  2. 整理“当前实现 vs 截图规划”的差异
  3. 决定设置页是否需要新增/调整：
     - 左侧 provider 列表是否和 mock 一致
     - provider 启用/停用按钮是否清晰
     - 拖动排序是否直观
     - 当前供应商账号卡片是否支持“测试连接/设为启用/新增账号”
     - 凭据表单是否符合“前端只显示脱敏，Worker 抓取时读取”的安全承诺
     - 百炼是否要特殊展示为“原网页入口型 provider”
  4. 将阿里云百炼卡片前端文案优化成稳定入口语义，避免用户误解为 API 抓取失败
- 完成后验证：
  - `npm run test`
  - `npm run build`
  - 线上：
    - `POST https://apimonitor.jarvislee90s.workers.dev/api/refresh`
    - 预期 OpenRouter/OpenCode/讯飞为 `ready`
    - 预期阿里云百炼为 `partial` 且 summary 是原网页入口说明
  - 前端：
    - 访问 `https://apimonitor.jarvislee90s.workers.dev/#/settings`
    - 检查桌面和移动端布局不重叠
- 回滚方案：
  - Worker 回滚：`npx wrangler@4 deployments rollback`
  - 代码回滚：`git revert <commit>`
  - 若百炼入口模式导致问题，可在 `worker/providers/aliyun-bailian.ts` 恢复旧 adapter，但不建议恢复默认云端抓取

### 5. 禁止丢失的信息（CRITICAL — PRESERVE VERBATIM）

```
用户原始需求精确措辞：
- "我想做一个 codingplan和api聚合站的用量看板"
- "具体包括讯飞maas codingplan（https://maas.xfyun.cn/packageSubscription）、opencodego codingplan（https://opencode.ai/workspace/wrk_01KTNPYQAX7HWSC5B04H1NEBRG/go），还有纯api调用的（https://openrouter.ai/activity）"
- "讯飞的网站登录需要点验证码，opencode和openrouter的登录是用github登录的，我github又是用google登录的"
- "可能偏向于用一个类似浏览器模拟的功能存储登录信息"
- "我也希望手机能使用（比如直接部署纯前端网页？）"
- "定时任务不用corn触发，打开前端访问的时候才触发任务刷新（每2min一次）。然后如果前端页面10min都没交互了就不再刷新了"
- "我还是想任务都集成在云端，不想给本地开一个http服务了"
- "我有supabase，可以用他的edgefuction抓取数据，然后反馈到前端页面吗？"
- "我有cloudflare可以部署"
- "增加阿里云百练 https://bailian.console.aliyun.com/cn-beijing?tab=plan#/efm/subscription/coding-plan"
- "opencode的重置时间能否改为北京时间？我看这个好像是默认数据库格林威治时间。前后端都要修改"
- "首页顶端看板增加配置按钮。目前三个供应商以按钮形式配置"
- "每个供应商的账户密码和apikey啥的能不能做成可填配置项，在单一供应商切换账户时填写"
- "方案C 安全增强版 / B 式体验"
- "行吧，那你先做这个稳妥的版本; 然后第2个问题的话，就是你看我这个截图规划里应该有写的，就是设置界面要做一个这种的看板，你找一下之前的记录啊;今天就先不做了吧，然后你把之前关于这个的记录整理一下，然后你handoff到这里"

百炼最终产品决策：
- 阿里云百炼默认作为“原网页入口型 provider”
- ApiMonitor 卡片只负责统一入口和说明
- 登录态由用户当前浏览器保存
- 如果浏览器阿里云登录没过期，点击原网页入口可直接看到订阅页
- 如果登录过期，用户在阿里云原网页重新扫码/登录
- 不再默认使用 Worker 纯 HTTP 复放百炼接口

设置页截图规划要点：
- 标题方向："独立配置页：C 安全增强版 / B 式体验"
- 左侧：供应商列表，OpenRouter / OpenCode Go / 讯飞 MaaS / 阿里云百炼，每个可启用/禁用
- 中间：看板展示预览，展示拖拽排序后的 provider 顺序
- 中间：当前供应商账号卡片，例如账号 A 主账号 ready 当前启用，账号 B 备用账号 login_required
- 操作：新增账号、测试连接、设为启用
- 下方：凭据配置表单，字段包括账号别名、workspaceId、Cookie/API Key、endpoint/baseUrl
- 安全提示：前端只显示脱敏值；明文仅 Worker 抓取时读取；不保存网页登录密码

wrangler.toml 完整内容：
name = "apimonitor"
main = "./worker/index.ts"
compatibility_date = "2026-06-11"

[vars]
OPENROUTER_BASE_URL = "https://openrouter.ai"
OPENCODE_GO_BASE_URL = "https://opencode.ai"
XFYUN_MAAS_PAGE_URL = "https://maas.xfyun.cn/packageSubscription"

[[durable_objects.bindings]]
name = "REFRESH_SESSION"
class_name = "RefreshSessionDurableObject"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["RefreshSessionDurableObject"]

[assets]
directory = "./frontend/dist"
not_found_handling = "single-page-application"
run_worker_first = ["/api/*"]

Supabase 项目: cdzjuvewreribelvpccz
Cloudflare Worker: apimonitor.jarvislee90s.workers.dev
Cloudflare Account ID: 5317e41898b6988237db8fad267788be
当前 Worker Version ID: 26acad0d-c6b8-4aed-a4c6-8eecd8e52ce6
```
