# CONTEXT HANDOFF — DO NOT SUMMARIZE, DO NOT COMPRESS

## 1. 任务状态（Task State）

- 当前目标：完成分层供应商账号配置页的**视觉呈现**对齐参考设计图，当前功能链路已通但 UI 层与设计差距大。
- 已完成步骤（本轮续作）：
  - commit `0189a92`：后端 `selectedAccountId` 接入 `activeProviderAccountId`
    - `worker/dashboard.ts`：`ProviderDashboardPreference` 加 `activeProviderAccountId`，`buildCard` 接受 preference 参数
    - `worker/index.ts`：`DashboardPreference` 类型同步
    - 新建 `tests/worker/dashboard.test.ts`：3 个用例
    - 修复 `tests/worker/index.test.ts` fixture 适配新类型
  - commit `453b80c`：Level 2 账号面板状态标签 + 新增账号按钮
    - 新建 `frontend/src/settings/account-status-labels.ts`：后端 status → 中文标签映射
    - `frontend/src/settings/settings-page.tsx`：账号项加状态标签、底部加"+ 新增账号"按钮
    - `frontend/src/styles.css`：状态标签样式 + 新增账号虚线按钮
    - `tests/frontend/settingsPage.test.tsx`：2 个新用例
  - commit `8027bd6`：计划文档 `docs/superpowers/plans/2026-06-13-layered-settings-remaining.md`
  - 部署完成：`npm.cmd run deploy:worker` 成功，Cloudflare Worker URL 为 `https://apimonitor.jarvislee90s.workers.dev`，Current Version ID 为 `fbf13ed7-a759-4864-b23a-990b28e133c5`。
  - 验证完成：`npm.cmd run test` 通过，11 个测试文件、69 个用例通过。
  - 验证完成：`npm.cmd run build` 通过，TypeScript + Vite build 成功。
- 进行中步骤：
  - **视觉呈现对齐参考设计图**——用户明确指出当前 Level 2 和 Level 3 跟设计图不一样，需要重写 UI 层。
- 阻塞项：
  - 无技术阻塞。
  - ⚠️ **视觉差距是主要阻塞**——当前实现功能正确但 UI 层级、卡片样式、表单布局与参考设计图不符。

## 2. 关键决策与假设（Decisions & Assumptions）

- 为什么选方案 A 而非 B：
  - （继承前序 handoff 的决策，不再重复）
- 已确认的环境假设：
  - 当前日期：2026-06-13。
  - 当前工作目录：`E:\LLMproject\ApiMonitor`。
  - Shell：PowerShell；实际运行命令使用 `npm.cmd`。
  - 云端 endpoint：`https://apimonitor.jarvislee90s.workers.dev/#/settings`。
  - Cloudflare Worker 部署版本：`fbf13ed7-a759-4864-b23a-990b28e133c5`。
- 尚未验证的假设：
  - ⚠️ 视觉对齐后需要用户在浏览器用真实 `ADMIN_SETUP_TOKEN` 做最终验收。
  - ️ Vite chunk size warning 尚未优化。
  - ⚠️ Recharts happy-dom warning 未消除。

## 3. 文件与代码状态（File & Code State）

- 已提交文件（本轮 3 个 commit）：
  - `worker/dashboard.ts` — `buildCard` 接受 preference，`selectedAccountId` 跟随 `activeProviderAccountId`
  - `worker/index.ts` — `DashboardPreference` 加 `activeProviderAccountId`
  - `tests/worker/dashboard.test.ts` — 新建，3 个用例
  - `tests/worker/index.test.ts` — fixture 适配
  - `frontend/src/settings/account-status-labels.ts` — 新建，status → 中文标签映射
  - `frontend/src/settings/settings-page.tsx` — Level 2 状态标签 + 新增按钮
  - `frontend/src/styles.css` — 状态标签 + 新增按钮 CSS
  - `tests/frontend/settingsPage.test.tsx` — 2 个新用例
  - `docs/superpowers/plans/2026-06-13-layered-settings-remaining.md` — 计划文档
- 关键变量/常量值：
  - 云端 URL：`https://apimonitor.jarvislee90s.workers.dev`
  - 云端部署 Version ID：`fbf13ed7-a759-4864-b23a-990b28e133c5`
- 未提交的变更：
  - 无（所有改动已提交）

## 4. 待办与下一步（Next Actions）

### 视觉差距清单（用户反馈，必须对齐参考图）

**Level 2 账号面板差距：**
- ❌ 当前：账号项是扁平 `<article>` 列表，只有文本 + 按钮
- ✅ 参考图：账号是**卡片式**呈现
  - 主账号：蓝色边框高亮卡片，内含 `API Key: sk-or...9a2f` 脱敏预览 + 彩色凭据块 + "首页显示" badge
  - 备用账号：灰色卡片，`API Key: sk-or...88bb` + "停用" badge
  - "+ 新增账号"：卡片底部虚线按钮 + 说明文字"新增后默认不进首页，测试通过后可启用。"

**Level 3 账号配置表单差距：**
- ❌ 当前：单列 `<label>` 垂直堆叠（账号名称 / 原网页 / 各凭据字段 / 保存按钮）
- ✅ 参考图：**双列网格布局**
  - 第一行：账号别名 | 首页显示开关
  - 第二行：API Key / Cookie | endpoint / baseUrl
  - 第三行：原网页链接 | 测试连接状态
  - 状态指示彩色块（蓝/灰/灰）
  - 安全提示区域：「保存后表单清空；账号卡只展示 credentialHint，网页登录态不保存。」

**Level 2 标题差距：**
- ❌ 当前：`Level 2: 当前供应商的账号`（通用）
- ✅ 参考图：`Level 2: OpenRouter 的账号`（跟随当前选中供应商动态变化）

**Level 2 说明文案差距：**
- ❌ 当前：`选择要在首页大卡片内显示的账号，停用不会删除配置。`
- ✅ 参考图：`已配置账号统一显示在该供应商下；停用只影响首页，不删除配置。`

### 下一步行动

1. **重写 Level 2 账号卡片样式** — 从扁平列表改为卡片式，主账号高亮 + 凭据脱敏预览 + 状态 badge
2. **重写 Level 3 表单布局** — 从单列改为双列网格，加入测试连接状态行和安全提示
3. **动态化 Level 2 标题** — 改为 `Level 2: {providerName} 的账号`
4. **验证** — `npm run test` + `npm run build` + `npm run deploy:worker`
5. **用户手动验收** — 浏览器输入真实 token 确认视觉对齐

### 完成后验证：
- `npm.cmd run test`，预期：11 个测试文件、69+ 用例通过
- `npm.cmd run build`，预期：TypeScript 和 Vite build 成功
- `npm.cmd run deploy:worker`，预期：部署成功
- Playwright MCP 截图与参考图人工对照

### 回滚方案：
- 如视觉改动过大影响现有功能：保留功能代码不变，只在 CSS 层做样式调整
- 如双列布局影响移动端：保留功能完整性，移动端改为纵向堆叠

## 5. 禁止丢失的信息（CRITICAL — PRESERVE VERBATIM）

```text
用户原始需求（视觉对齐反馈），必须原样保留：

"这个跟我当时设计页面不一样"
参考图关键特征：
- Level 2 账号是卡片式，不是扁平列表
- 主账号蓝色边框高亮 + API Key 脱敏预览 + "首页显示" badge
- 备用账号灰色 + "停用" badge
- Level 2 标题跟随供应商动态变化："Level 2: OpenRouter 的账号"
- Level 3 表单是双列网格：账号别名|首页显示开关、API Key|endpoint、原网页|测试连接
- 安全提示：「保存后表单清空；账号卡只展示 credentialHint，网页登录态不保存。」

关键错误日志与验证输出，必须原样保留：

PowerShell npm.ps1 执行策略错误：
npm : 无法加载文件 C:\Program Files\nodejs\npm.ps1，因为在此系统上禁止运行脚本。有关详细信息，请参阅 https:/go.microsoft.com/fwlink/?LinkID=135170 中的
about_Execution_Policies。
所在位置 行:2 字符: 1
+ npm run test:frontend -- tests/frontend/settingsPage.test.tsx
+ ~~~
    + CategoryInfo: SecurityError: (:) []，PSSecurityException
    + FullyQualifiedErrorId: UnauthorizedAccess

旧本地代理/云端链路错误：
[GET] http://127.0.0.1:5174/api/settings/providers => [500] Internal Server Error
页面提示：
请求失败 (500)

Vite config 关键片段：
const apiProxyTarget = env.VITE_API_PROXY_TARGET ?? "http://127.0.0.1:8787";

部署输出关键行（最新版本）：
Uploaded apimonitor (10.15 sec)
Deployed apimonitor triggers (2.05 sec)
  https://apimonitor.jarvislee90s.workers.dev
Current Version ID: fbf13ed7-a759-4864-b23a-990b28e133c5

测试输出关键行（最新）：
Test Files  11 passed (11)
Tests  69 passed (69)

构建 warning 原文：
(!) Some chunks are larger than 500 kB after minification. Consider:
- Using dynamic import() to code-split the application
- Use build.rollupOptions.output.manualChunks to improve chunking: https://rollupjs.org/configuration-options/#output-manualchunks
- Adjust chunk size limit for this warning via build.chunkSizeWarningLimit.

Recharts 测试 warning 原文：
The width(0) and height(0) of chart should be greater than 0,
       please check the style of container, or the props width(100%) and height(100%),
       or add a minWidth(0) or minHeight(undefined) or use aspect(undefined) to control the
       height and width.
The width(0) and height(0) and height(0) of chart should be greater than 0,
       please check the style of container, or the props width(100%) and height(100%),
       or add a minWidth(0) or minHeight(undefined) or use aspect(undefined) to control the
       height and width.
```

---

**Handoff 规则**：
1. 上述 5 个 section 是最小集，缺一不可。
2. 第 5 节内容用代码块包裹，防止被 token 压缩。
3. 每次继续任务前，先读取本 handoff，再决定下一步。
4. 任务结束后删除本节，避免污染后续上下文。
